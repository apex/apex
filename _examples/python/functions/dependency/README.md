Install required libraries using pip. You need to install these libraries at a level of a function.

```
pip install requests -t functions/dependency
```

The same could be accomplished with `requirements.txt`